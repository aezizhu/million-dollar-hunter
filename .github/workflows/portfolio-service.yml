name: portfolio-service

on:
  push:
    paths:
      - 'services/portfolio-service/**'
      - '.github/workflows/portfolio-service.yml'
  pull_request:
    paths:
      - 'services/portfolio-service/**'
      - '.github/workflows/portfolio-service.yml'

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.x'

      - name: Build
        run: |
          # Ensure go.work is respected for monorepo local modules
          go work use ./services/portfolio-service ./services/market-data-service || true
          cd services/portfolio-service
          go build ./...

      - name: Test
        run: |
          go work use ./services/portfolio-service ./services/market-data-service || true
          cd services/portfolio-service
          go test ./...
